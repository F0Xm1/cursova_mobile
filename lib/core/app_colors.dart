import 'package:flutter/material.dart';

class AppColors {
  // Основний фон (темний)
  static const Color background = Color(0xFF121212);

  // Текст (білий для темної теми)
  static const Color text = Colors.white;
  static const Color textSecondary = Colors.grey;

  // Акцентний колір (помаранчевий з макетів)
  static const Color primary = Color(0xFFE87A38);
  static const Color buttonText = Colors.white;

  // Поля вводу
  static const Color inputBackground = Color(0xFF1E1E1E);
  static const Color inputBorder = Colors.transparent; // Без рамки, тільки заливка

  static const Color error = Colors.redAccent;

  // Premium кольори
  static const Color premiumGold = Color(0xFFD4AF37);

  // UI елементи
  static const Color cardShadow = Color(0x1A000000);
  static const Color sponsoredBanner = Color(0xFF333333);
}